<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="css/bootstrap.min.css" />

    <title>Request information</title>
  </head>
  <body>

    
    <div
      class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow"
    >
      <h5 class="my-0 mr-md-auto font-weight-normal">Squeak!</h5>

      <form class="form-inline w-75 mt-2 mt-md-0" method="POST" action="/home">
        <div class="input-group mr-sm-2 w-75">
          <div class="input-group-prepend">
            <div class="input-group-text">{{currentUser}}</div>
          </div>
          <input type="hidden" name="csrf" value="{{token}}">
          <input
            type="text"
            name="squeak"
            class="form-control"
            id="squeak"
          />
          <select class="custom-select" id="inputGroupSelect01" name="receiver">
          <option selected value="@everyone">@everyone</option>
          {{#usernames}}
            <option value={{username}}>{{username}}</option>
            {{/usernames}}
          </select>
        </div>
        <button type="submit" class="btn btn-primary mr-sm-2 disabled">
          Squeak!
        </button>
      </form>

      <form class="form-inline mt-2 mt-md-0" action="/signout" method="GET">
        <button type="submit" class="btn btn-primary">Sign out</button>
      </form>
    </div>
    
    <div class="container"><h1>Squeaks</h1></div>
    <div class="container">{{#posts_sqks}}
    <div class="card mb-2">
    <div class="card-header">
      {{username}}
      <span class="float-right">{{time}}</span>
    </div>
    <div class="card-body">
      <p class="card-text">
      {{post}}
      </p>
    </div>
  </div>
    
    {{/posts_sqks}}</div>

    <div class="container"><h1>Squeals</h1></div>
    <div class="container">{{#posts_sqls}}
    <div class="card mb-2">
    <div class="card-header">
      {{username}}
      <span class="float-right">{{time}}</span>
    </div>
    <div class="card-body">
      <p class="card-text">
      {{post}}
      </p>
    </div>
  </div>
    
    {{/posts_sqls}}</div>

    <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    <script src="js/jquery-3.3.1.min.js"></script>
    <script src="js/popper.min.js"></script>
    <script src="js/bootstrap.min.js"></script>
    <script src="js/squeak.js"></script>
  </body>
</html>
